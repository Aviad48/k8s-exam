#1 kubectl get pods --show-labels

#2 Apply the yaml file "Questions-2.yaml"

#3 kubectl get pods --show-labels

#4 kubectl get pods -l env=dev


#5 kubectl get pods -l env=dev -o custom-columns=NAME:.metadata.name,LABELS:.metadata.labels

#6  kubectl get pods -l env=prod --show-labels

#7 kubectl get pods -l env=prod -o custom-columns=NAME:.metadata.name,LABELS:.metadata.labels

#8 kubectl get pods -l env --show-labels

#9 kubectl get pods -l 'env in (dev,prod)' --show-labels

#10 kubectl get pods -l 'env in (dev,prod)' --no-headers -o custom-columns=LABELS:.metadata.labels


#11 kubectl label pod nginx-prod-1 env=uat --overwrite
#kubectl get pods --show-labels

#12 kubectl label pods -l 'env in (dev,prod,uat)' env-
#kubectl get pods --show-labels

#13 kubectl label pods --all app=nginx
#kubectl get pods --show-labels

#14 kubectl get nodes --show-labels

#15 kubectl label node <node-name> nodeName=nginxnode


#16 apply yaml file "question16.yaml" by kubectl apply -f question16.yaml

#17 kubectl get pod nginx  -o wide

#18  kubectl get pod nginx -o wide
# kubectl describe pod nginx | grep "Node-Selectors"

#################
# Deployments: 
#################


#1 Apply deployment-nginx.yaml 

# kubectl create deploy webapp --image=nginx --dry-run=client -o yaml > webapp.yaml
#  sed -i 's/replicas: 1/replicas: 5/' webapp.yaml

#2 kubectl rollout status deployment/webapp

#3 kubectl get replicaset -l app=webapp
#kubectl get rs

#4 kubectl get replicaset -l app=webapp -o yaml > replicaset.yaml (Yaml file is in the folder by name replicaset.yaml )
#  kubectl get pods -l app=webapp -o yaml > pods.yaml (Yaml file is in the folder by name pods.yaml)


#5 kubectl delete deployment webapp --cascade=true --wait=true

#6 apply yaml file "deploy-nginx-1.17.1.yaml"

#7 kubectl set image deployment webapp nginx=nginx:1.17.4
# kubectl rollout status deployment webapp

#8 kubectl rollout history deployment webapp

#9 kubectl rollout undo deployment webapp
#  kubectl rollout status deployment webapp
# kubectl get deployment webapp -o=jsonpath='{.spec.template.spec.containers[0].image}' && echo

#10 kubectl set image deployment webapp nginx=nginx:1.100
# kubectl get pods
# kubectl rollout undo deployment webapp
# kubectl rollout status deployment webapp
# kubectl get pods
# kubectl rollout history deploy webapp --revision=4 (outpot in history revision.txt file)
# kubectl set image deployment webapp nginx=nginx:latest
# kubectl rollout history deploy webapp --revision=6




#11  Apply yaml file "autoscaling-deploy.yaml" 
# Watch the HPA (HorizontalPodAutoscaler) and see the replica count changing
#    watch kubectl get hpa,pods

#12 Delete the HorizontalPodAutoscaler (HPA)
#   kubectl delete hpa webapp-hpa
#
# Delete the Deployment
#   kubectl delete deployment webapp





 





























